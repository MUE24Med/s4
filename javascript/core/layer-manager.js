// ============================================
// layer-manager.js - نسخة مبسطة (بدون نقل عناصر)
// ============================================

export function hideOverlappingGroupImages() {
    // معطل مؤقتاً - تجنب التسبب في مشاكل الإحداثيات
    console.log("⚠️ hideOverlappingGroupImages معطلة مؤقتاً");
}

export function bringSectionRectsToFront() {
    // معطل مؤقتاً - تجنب نقل المستطيلات خارج مجموعاتها
    console.log("⚠️ bringSectionRectsToFront معطلة مؤقتاً");
}